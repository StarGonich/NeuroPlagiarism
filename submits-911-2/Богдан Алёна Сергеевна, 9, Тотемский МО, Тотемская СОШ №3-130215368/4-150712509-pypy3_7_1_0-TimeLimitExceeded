import math

def distance(x1, y1, x2, y2):
    return math.sqrt((x1 - x2)**2 + (y1 - y2)**2)

def solve():
    n = int(input())
    points = []
    for _ in range(n):
        x, y, c = map(int, input().split())
        points.append((x, y, c))

    for k in range(1, n, 2):
        correct_count = 0
        for i in range(n):
            # LOO - исключаем текущую точку
            distances = []
            for j in range(n):
                if i != j:
                    dist = distance(points[i][0], points[i][1], points[j][0], points[j][1])
                    distances.append((dist, points[j][2]))

            # Находим k ближайших точек
            distances.sort()
            neighbors = distances[:k]

            # Определяем цвет большинства среди k ближайших
            black_count = sum(1 for _, color in neighbors if color == 1)
            white_count = k - black_count
            predicted_color = 1 if black_count > white_count else 0

            # Сравниваем с фактическим цветом и увеличиваем счетчик
            if predicted_color == points[i][2]:
                correct_count += 1

        print(correct_count)

solve()
