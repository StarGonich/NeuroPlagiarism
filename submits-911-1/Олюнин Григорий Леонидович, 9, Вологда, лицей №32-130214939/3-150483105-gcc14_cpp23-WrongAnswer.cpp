#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
    ll n, t;
    cin >> n >> t;
    vector<ll> t1(n);
    vector<ll> t2(n);
    for (int i = 0; i < n; ++i){
        cin >> t1[i] >> t2[i];
    }
    cout << -1;
    return 0;
}
