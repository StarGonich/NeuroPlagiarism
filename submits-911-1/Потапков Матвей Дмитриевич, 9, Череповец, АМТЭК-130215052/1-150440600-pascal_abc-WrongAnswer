program n1;
var h1,m1,h2,m2,i,i2,q:integer;
  begin
    read(h1,m1,h2,m2);
    for i:=h1 to h2 do begin
       for i2:=m1 to m2 do begin
         if (i=i2) then q:=q+1;
          end; end;
          writeln(q);
  end.