#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using ld = long double;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int k, n, m;
    cin >> k >> n >> m;
    int dd;
    for(int i = 0; i < k; i++) {
        for(int j = 0; j < n; j++) {
            for(int f = 0; f < m; f++){
            int a;
            cin >> a;
            if(a) dd = i;}
        }
    }
    if(dd == 0) {
        cout << "1 2";
    }
    else {cout << "2 1";}
    return 0;
}
